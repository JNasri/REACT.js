// to use a snippet (re-usable code)
// Ctrl + Alt + R 
// then type rafce
import React from "react";

const Header = () => {
  return (
    <header>
      <h1>Books List</h1>
    </header>
  );
};

export default Header;
